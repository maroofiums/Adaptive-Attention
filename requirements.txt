torch>=2.0.0
numpy>=1.24.0
matplotlib>=3.7.0
jupyter>=1.0.0
nbformat>=5.9.0
pytest>=7.4.0

# Optional, for reproducing full paper experiments with real data:
# datasets>=2.14.0          # Hugging Face datasets (SST-2, MNLI, CoNLL-2003)
# transformers>=4.35.0      # tokenizers
# entmax>=1.1               # alpha-entmax baseline (https://github.com/deep-spin/entmax)
# scikit-learn>=1.3.0       # bootstrap utilities / metrics
